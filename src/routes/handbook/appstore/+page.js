
// import { getStores, navigating, page, updated } from '$app/stores';
export function load() {
  // load() 함수에서는 사용할 수 없습니다. 다음과 같은 오류가 발생합니다. 
  // Error: Function called outside component initialization
  ///console.log(getStores().page);
}